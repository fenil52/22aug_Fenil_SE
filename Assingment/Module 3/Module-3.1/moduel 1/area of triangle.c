#include<stdio.h>
void main()
{
    float b,h,area;
    printf("enter the value of b:");
    scanf("%f",&b);
    printf("enter the value of h:")
    scanf("%f",&h);
    float area=(b*h)/2;
    printf("\narea of triangle is:%f",area);
}